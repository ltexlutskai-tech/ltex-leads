# L-TEX CRM — перепризначення менеджера (v6.9)

Модуль `ManagerTransfer.gs` додає в Apps Script-проєкт CRM автоматичний
**перенос контрагента між менеджерами**: змінюєш менеджера в колонці **K**
аркуша `🔒 2026` — рядок сам зникає з таблиці попереднього менеджера,
зʼявляється в таблиці нового, а обидва отримують повідомлення у Viber.

## Що відбувається при зміні менеджера

1. Рядок з цим `ID` **видаляється** з файлів усіх інших менеджерів
   (в тому числі тих, кого позначено як неактивних).
2. Дані, які менеджер уже встиг заповнити у своєму файлі —
   `Перший контакт`, `Активність`, `Статус дії`, `Оновлений статус`,
   `Коментар` — **не губляться**: підтягуються в головну таблицю
   (якщо там порожньо) і їдуть у файл нового менеджера.
3. Рядок додається у файл нового менеджера (дублі з тим самим `ID`
   прибираються автоматично).
4. **Новому менеджеру** у Viber:
   `📥 Вам передано контрагента! … ⚠️ Потрібно актуалізувати інформацію та продовжити роботу.`
5. **Попередньому менеджеру**: `📤 Контрагента передано іншому менеджеру… Рядок прибрано з вашої таблиці.`
6. Керівникам (`notifyOwners`) — коротка довідка «було → стало»,
   а в головний файл пишеться прихований журнал `_transfers`.
7. Якщо налаштований місток `LTEX_CRM_URL` — зміна менеджера їде і в нашу систему.

Перше призначення менеджера (раніше клітинка була порожня) переносом
**не вважається** — там працює звичне сповіщення «Новий клієнт призначено тобі!»
з `onEdit` у `Code.gs`.

## Встановлення

1. Відкрий **той самий проєкт Apps Script, де лежить код бота** — у ньому є
   функції `doPost`, `parseAndSave`, `getManagers`, `sendViber`, і саме він
   опублікований як веб-застосунок (його адреса = `WEBHOOK_URL`).

   ⚠️ Це **не** скрипт, прив’язаний до таблиці (`Розширення → Apps Script`
   з відкритої таблиці) — то окремий порожній проєкт, де немає коду CRM.
   Якщо вставити модуль туди, буде помилка
   `ReferenceError: getManagers is not defined`.

2. Створи в цьому проєкті новий файл скрипта **`ManagerTransfer`**
   і встав туди вміст `ManagerTransfer.gs`.
3. Запусти **`checkTransferSetup()`** — має бути `✅` навпроти всіх функцій CRM.
4. Запусти один раз функцію **`installManagerTransferTrigger()`**
   (дозволи доступ, якщо запитає).
5. Перевір: `Тригери` → має зʼявитись `onMainEditTransfer` для головної таблиці.

Існуючий код чіпати не потрібно — `onEdit(e)` у `Code.gs` лишається як є.
Модуль працює через власний тригер і використовує наявні
`getManagers()`, `syncToManager()`, `sendViber()`, `notifyOwners()`,
`pushLeadToLtexCrm()`, `generateId()`.

Прибрати тригер: `removeManagerTransferTrigger()`.

### Якщо щось не працює

Запусти **`checkTransferSetup()`** — у логах буде видно, які константи модуль
бачить, чи відкривається головна таблиця, чи на місці функції CRM
(`getManagers`, `syncToManager`, `sendViber`…) і чи встановлено тригер.

> **Чому модуль має власні копії констант.**
> У рантаймі V8 глобальні `const`/`let` з одного файлу `.gs` не завжди видно
> в іншому — звідси помилка `ReferenceError: MAIN_FILE_ID is not defined`.
> Тому модуль читає значення з `Code.gs`, якщо вони доступні, а якщо ні —
> бере запасні копії з блоку `TR_*` на початку файлу (функція `TR()`).
> Функції між файлами видно завжди, тож їх дублювати не потрібно.
>
> ⚠️ Якщо в `Code.gs` колись зміниться `MAIN_FILE_ID`, назва аркуша
> або `ADMIN_VIBER_ID` — онови й запасні значення `TR_*`.

## Команда бота `/передати`

Дозволяє перекинути контрагента прямо з Viber, без відкривання таблиці.

**Уже увімкнена** у `Code.gs` цього репозиторію (v6.9): у `doPost` одразу після
блоку команди `/1с` стоїть

```js
if (tl.startsWith("/передати")||tl.startsWith("/передать")||tl.startsWith("/transfer")) {
  handleTransferCommand(text, sender); return okResponse();
}
```

плюс рядок про неї в тексті `/допомога`. Це єдині дві правки v6.9 у `Code.gs` —
решта файлу дослівно збігається з v6.8.

⚠️ Після оновлення `Code.gs` треба **передеплоїти веб-застосунок**:
`Розгорнути → Керувати розгортаннями → олівець → Версія: Нова версія → Розгорнути`.
Адреса (`WEBHOOK_URL`) при цьому не змінюється.

**Формати:**

```
/передати 0671234567 Дунас Богдан
/передати LTEX-20260101-1234 Дунас

/передати
Телефон: 0671234567
Менеджер: Дунас Богдан
```

Імʼя менеджера можна писати неповністю (`Дунас`, `богдан`) — якщо збігів кілька,
бот попросить уточнити і покаже варіанти. Клієнта шукає за номером телефону
(останні 9 цифр) або за `ID`.

### Повторний запит

Коли клієнт уже є в CRM за іншим менеджером і звернувся знову — додай після
імені менеджера через `|` (або з нового рядка) короткий опис нового звернення:

```
/передати 0671234567 Дунас Богдан | Питає ціну на палету, писав у TikTok

/передати
Телефон: 0671234567
Менеджер: Дунас Богдан
Запит: Питає ціну на палету, писав у TikTok
```

Що відбувається додатково до звичайної передачі:

1. Новий менеджер отримує **`🔁 ПОВТОРНИЙ ЗАПИТ — вам передано контрагента!`**,
   де першим рядком іде текст нового звернення.
2. Опис дописується в колонку **«Цікавить»** головної таблиці окремим рядком
   `🔁 Повторний запит 20.05.2026 (хто передав): …` — стара інформація
   зберігається, виходить історія звернень. Вона ж їде у файл нового менеджера.
3. Статус повертається в **«Очікує»** — клієнт знову в роботі й потрапляє
   в щоденний звіт.
4. Попередній менеджер бачить причину передачі, керівники — позначку
   `🔁 Повторний запит + перепризначення`, а в журналі `_transfers`
   зʼявляється колонка «Повторний запит» з описом.

Без опису команда працює як звичайна передача — жодних додаткових позначок.

**Права доступу:**

| Хто | Що може |
|---|---|
| Адміністратор (`ADMIN_VIBER_ID`) і керівники (`NOTIFY_IDS`) | передати будь-якого контрагента |
| Менеджер | передати тільки клієнта, закріпленого за ним |
| Стороння людина | нічого — бот відповість, що команда лише для менеджерів |

Далі працює той самий механізм переносу: рядок зникає у попереднього менеджера,
зʼявляється у нового з усіма даними обдзвону, обидва отримують повідомлення
(ініціатору команди дубль не надсилається — він бачить підтвердження у відповіді).

## Ручні інструменти (запуск з редактора)

| Функція | Що робить |
|---|---|
| `reassignLead("LTEX-…", "Дунас Богдан")` | Перепризначити конкретного ліда за ID |
| `reassignLead("LTEX-…", "Дунас Богдан", "Питає ціну")` | Те саме з позначкою повторного запиту |
| `reassignLeadByPhone("0671234567", "Дунас Богдан")` | Те саме за номером телефону (третім аргументом теж можна опис) |
| `cleanupManagerFilesFromMain(true)` | **Перевірка**: показує, скільки «чужих» рядків у кожного менеджера, нічого не змінює |
| `cleanupManagerFilesFromMain(false)` | Прибирає з файлів менеджерів рядки, які за головною таблицею належать іншому |

`cleanupManagerFilesFromMain` варто запустити один раз після встановлення —
вона наведе лад після попередніх ручних перенесень. Рядки, `ID` яких немає
в головній таблиці, вона **не видаляє** — лише рахує у звіті.

## Налаштування у файлі

| Константа | За замовчуванням | Призначення |
|---|---|---|
| `TRANSFER_LOG_SHEET` | `_transfers` | Назва аркуша-журналу перенесень |
| `TRANSFER_MAX_ROWS` | `50` | Ліміт рядків за одне редагування (захист від таймауту при вставці великого діапазону) |
| `TRANSFER_PUSH_TO_CRM` | `true` | Чи надсилати зміну менеджера в L-TEX CRM |

## Тести

Логіку переносу перевірено на емуляції Google Apps Script (без доступу до реальних таблиць):

```bash
node apps-script/test/transfer.test.js      # логіка переносу і команди
node apps-script/test/integration.test.js   # Code.gs + ManagerTransfer.gs разом
```

Покрито: перенос із збереженням даних обдзвону, відсутність зайвих сповіщень
при першому призначенні, ігнорування редагувань інших колонок, прибирання
дублів, забирання рядка в неактивного менеджера, `reassignLead`,
`cleanupManagerFilesFromMain`, а також команда `/передати` — права доступу,
пошук за телефоном/ID, неповне імʼя менеджера, багаторядковий формат
і всі помилкові сценарії, а також робота модуля в ситуації, коли константи
з `Code.gs` недоступні.

Інтеграційний тест ганяє **справжні** `Code.gs` і `ManagerTransfer.gs` разом:
`/передати` через `doPost`, наявність команди в `/допомога`, підказку при
порожній команді, усі три формати повторного запиту (`|`, поле `Запит:`,
новий рядок) і те, що `/менеджери` та автоперевірка номера не зламані.

## Файли

| Файл | Що це |
|---|---|
| `Code.gs` | Основний код CRM, v6.9 (v6.8 + дві правки для `/передати`) |
| `ManagerTransfer.gs` | Модуль перепризначення менеджера |
| `test/` | Тести на емуляції Apps Script (Node.js) |
